<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="buttonStyle">@style/AppButtonStyle</item>
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="NavigationMenuStyle">
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/average_sans</item>
        <item name="android:textAlignment">center</item>
    </style>


    <style name="AlertDialogAnims">
        <item name="android:windowEnterAnimation">@anim/alert_dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/alert_dialog_exit_anim</item>
    </style>

    <style name="AppButtonStyle" parent="@style/Widget.MaterialComponents.Button">
        <item name="android:elevation">2dp</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/rounded_dialog</item>
    </style>

</resources>
